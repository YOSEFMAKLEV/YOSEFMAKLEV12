runtime: nodejs20
env: standard

instance_class: F2

automatic_scaling:
  min_instances: 0
  max_instances: 10

build_env_variables:
  PRISMA_CLI_BINARY_TARGETS: debian-openssl-3.0.x

env_variables:
  NODE_ENV: production
  NEXT_TELEMETRY_DISABLED: "1"
